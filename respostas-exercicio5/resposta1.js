let num = new Array(5);

for (let i = 0; i < num.length; i++){
    num[i] = prompt('Informe um número inteiro: ' );
    num[i] = parseInt(num[i]);
}

for (let i = 0; i < num.length; i++){
        alert(num[i]);
            
}
